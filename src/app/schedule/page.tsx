'use client';
import { useState } from 'react';
import { CalendarDays, Clock3, MapPin } from 'lucide-react';
import { StudentPage, SectionCard } from '@/components/student/student-page';
import { TIMETABLE, SUBJECTS, PERIOD_TIMES } from '@/lib/timetable';
export default function SchedulePage(){const [day,setDay]=useState('MON');return <StudentPage title="Schedule"><div className="mb-4 flex gap-2 overflow-x-auto student-scroll-x">{Object.keys(TIMETABLE).map(d=><button key={d} onClick={()=>setDay(d)} className={`min-w-[58px] rounded-2xl px-3 py-2 text-xs font-bold ${day===d?'bg-[#37336e] text-white':'bg-white text-slate-500 shadow-sm'}`}>{d}</button>)}</div><SectionCard title="II-A · Room 423" icon={CalendarDays}><div className="space-y-2">{TIMETABLE[day].map((code,i)=>{if(!code)return <div key={i} className="my-2 border-t border-dashed border-slate-200 pt-2 text-center text-[10px] text-slate-400">Lunch / Break</div>;const s=SUBJECTS[code];return <div key={i} className="flex items-center gap-3 rounded-xl bg-[#f8f8fb] p-3"><div className="w-16 text-[10px] font-semibold text-slate-400">{PERIOD_TIMES[i]?.startTime}</div><div className="flex-1"><p className="text-xs font-bold">{s?.name||code}</p><p className="mt-1 text-[10px] text-slate-400"><Clock3 className="mr-1 inline h-3 w-3"/>{PERIOD_TIMES[i]?.startTime} – {PERIOD_TIMES[i]?.endTime}</p></div><MapPin className="h-4 w-4 text-[#55a6bb]"/></div>})}</div></SectionCard></StudentPage>}
